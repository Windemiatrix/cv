import React from 'react';

const Profession = (props) => {
    return (
        <div className={"h6"} style={{marginBottom: "20px"}}>
            {props.content}
        </div>
    );
};

export default Profession;